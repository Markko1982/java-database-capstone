# MySQL (dev local via docker compose)
spring.datasource.url=jdbc:mysql://localhost:3306/cms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=cms
spring.datasource.password=cms

# usar schema.sql / data.sql no MySQL
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=always

# Mongo (dev local via docker compose)
spring.data.mongodb.uri=mongodb://localhost:27017/cms
